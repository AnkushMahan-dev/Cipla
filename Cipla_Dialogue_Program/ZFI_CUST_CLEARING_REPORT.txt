*&---------------------------------------------------------------------*
*& Include          SLIM_USER_CLF_HELP_E01
*&---------------------------------------------------------------------*
AT SELECTION-SCREEN ON VALUE-REQUEST FOR p_file.
  PERFORM f4_help_file CHANGING p_file.

AT SELECTION-SCREEN ON VALUE-REQUEST FOR p_imprt.
  PERFORM f4_help_file CHANGING p_imprt.

AT SELECTION-SCREEN OUTPUT.
  PERFORM selection_screen_output.

AT SELECTION-SCREEN ON VALUE-REQUEST FOR p_locf.
  PERFORM f4_help_locf using 'P_LOCF' CHANGING p_locf.

AT SELECTION-SCREEN ON p_file.
  PERFORM check_file_parameter.

AT SELECTION-SCREEN ON VALUE-REQUEST FOR p_locf2.
  PERFORM f4_help_locf using 'P_LOCF2' CHANGING p_locf2.

AT SELECTION-SCREEN ON p_profn.
  PERFORM check_selection_parameters.

AT SELECTION-SCREEN ON VALUE-REQUEST FOR p_locr.
  PERFORM f4_help_locr CHANGING p_locr.

AT SELECTION-SCREEN ON p_role.
  PERFORM check_selection_parameters.

AT SELECTION-SCREEN ON p_imprt.
  PERFORM check_selection_parameters.

AT SELECTION-SCREEN.
  PERFORM selection_screen.

INITIALIZATION.
  PERFORM init.

START-OF-SELECTION.
  PERFORM run.
AT USER-COMMAND.
  PERFORM user_command.
